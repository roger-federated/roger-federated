# Commands the agent must NEVER run.
[blocked]
rm -rf /
sudo
shutdown
reboot
mkfs
dd if=
format

# Commands that require user approval before running.
[confirm]
rm
pip install
git push
git reset
chmod
chown

# Substrings that trigger confirm regardless of position in the command (case-insensitive).
# Catches in-shell evasion primitives that prefix matching would miss.
# Note: bare '-e' omitted to avoid false positives (e.g. sed -e).
[evasion]
-enc
invoke-expression
iex
